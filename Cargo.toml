[package]
name = "rauc-health"
version = "0.1.0"
edition = "2021"
license = "MIT"


[dependencies]
clap = { version = "4", features = ["derive"] }
log = "0.4"
env_logger = "0.11"
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
once_cell = "1"

